<?xml version="1.0" encoding="utf-8"?>
<!-- Shown in the Android TV home screen's app row (required by the LEANBACK_LAUNCHER intent
     filter in AndroidManifest.xml). Built the same way as the adaptive launcher icon — the
     app's void background behind its foreground mark — rather than as new artwork, so the two
     stay visually consistent. Vector-composed instead of a rasterised PNG per density: a plain
     layer-list scales cleanly to whatever box the launcher gives it. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/ic_launcher_background" />
    <item
        android:width="108dp"
        android:height="108dp"
        android:gravity="center"
        android:drawable="@mipmap/ic_launcher_foreground" />
</layer-list>
